import { Create } from "ra-ui-materialui";
import { LocationsForm } from "./LocationsForm";

export const LocationsCreate = (props: any) => (
  <Create {...props}>
    <LocationsForm />
  </Create>
);
